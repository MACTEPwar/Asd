import { MenuItem2TreeNodePipe } from './menu-item2-tree-node.pipe';

describe('MenuItem2TreeNodePipe', () => {
  it('create an instance', () => {
    const pipe = new MenuItem2TreeNodePipe();
    expect(pipe).toBeTruthy();
  });
});
